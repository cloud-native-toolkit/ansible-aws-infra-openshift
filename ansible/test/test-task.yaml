---

- name: Parse vpc creation output
  hosts: localhost
  gather_facts: no

  vars:
    vpcData: "{{ lookup('file', './vpc-output.json') | from_json }}" 

  tasks:

  #- name: Get details of created VPCs
  #  amazon.aws.ec2_vpc_net_info:
  #    filters:
  #      "tag:Name": "fs-cloud-edge-vpc"
  #  register: vpcData


  - name: Show VPC id
    debug:
      msg: "VPC id is {{vpcData.vpc.id}}"