---

# Configuration variables for FS Cloud Build

# AWS Account Profile (set up in boto)
awsProfile: default

# Name tag prefix for the network that will be created
edge: fs-cloud-edge
management: fs-cloud-mgmt
workload: fs-cloud-workload

# Network addresses. Note that these must be at last /13 addresses and must not overlap
edge_cidr: 10.1.0.0/13
management_cidr: 10.10.0.0/13
workload_cidr: 10.40.0.0/13

